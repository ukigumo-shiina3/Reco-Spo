export const DEFAULT_AVATARS_BUCKET = 'avatars';

export type Post = {
  avatar_url: string;
  name: string;
  title: string;
  appeal: string;
  area: string;
  link: string;
  target_person: string;
  usage_fee: string;
  term: string;
  postal_code: string;
  address: string;
  manager: string;
  tel: string;
  email: string;
  id: number;
  prefectures_name: string;
  // system_name: string;
};
